---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: users.auth.alex123012.com
spec:
  group: auth.alex123012.com
  names:
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              databaseConfig:
                items:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              name:
                description: Name is appropriate database user name. If not provided
                  - operator automatically substitutes this parameter with CR name
                  (Comming soon) TODO
                type: string
              passwordSecret:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              privileges:
                description: Privileges
                items:
                  properties:
                    database:
                      type: string
                    "on":
                      description: if used PrivilegeType from PrivilegeTypeMap in
                        Privilege specify object to give Privilege to
                      type: string
                    privilege:
                      description: Privilege is role name or PrivilegeType
                      type: string
                  required:
                  - privilege
                  type: object
                type: array
              sslSecret:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
            required:
            - databaseConfig
            type: object
          status:
            description: UserStatus defines the observed state of User
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
