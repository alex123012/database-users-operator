---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: privilegesbindings.databaseusersoperator.com
spec:
  group: databaseusersoperator.com
  names:
    kind: PrivilegesBinding
    listKind: PrivilegesBindingList
    plural: privilegesbindings
    singular: privilegesbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrivilegesBinding is the Schema for the privilegesbindings API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PrivilegesBindingSpec defines the desired state of PrivilegesBinding.
            properties:
              databaseBindings:
                description: DatabaseBinding references to the DatabaseBinding that
                  will be used to apply privileges to user in a particular database
                items:
                  properties:
                    name:
                      description: resource name
                      type: string
                    namespace:
                      description: resource namespace
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              privileges:
                description: List of database privileges that will be applied to user.
                  If user already exists in database - all it privileges will be synchronized
                  with this list (all privileges that are not defined will be revoked).
                properties:
                  name:
                    description: resource name
                    type: string
                  namespace:
                    description: resource namespace
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - databaseBindings
            - privileges
            type: object
          status:
            description: PrivilegesBindingStatus defines the observed state of PrivilegesBinding.
            properties:
              summary:
                properties:
                  message:
                    type: string
                  ready:
                    type: boolean
                required:
                - ready
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
